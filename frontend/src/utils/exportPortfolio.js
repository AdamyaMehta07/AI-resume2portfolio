// Export is handled directly in PreviewPanel.jsx via generateHTML()
// This file is kept for backward compatibility
export function exportPortfolio(data, template) {
  console.log('Export handled by PreviewPanel')
}
